# Mockups

- `large-file-downloader-mockup.html` is a standalone browser mockup of the current SwiftUI window.
- Open it directly in a browser to review the layout without Figma.
- It mirrors the current app structure: header, left control column, right activity/log column, and the in-window action buttons.
